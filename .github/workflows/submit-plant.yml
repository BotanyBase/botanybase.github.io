name: Plant Submission Workflow
on:
  workflow_dispatch:
    inputs:
      name:
        type: string
      image:
        type: string
      climate:
        type: string
      description:
        type: string
jobs:
  create-issue:
    runs-on: ubuntu-latest
    env:
       PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      - name: Create Issue
        run: |
          echo "PAT_TOKEN: $PAT_TOKEN"
          curl -X POST \
          https://api.github.com/repos/BotanyBase/botanybase.github.io/issues \
          -H 'Authorization: Bearer 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' \
          -H 'Content-Type: application/json' \
          -d '{"title": "${name}", "body": "Image: ${image}\nClimate: ${climate}\nDescription: ${description}"}'
